dataset_id: your-dev-dataset-id
project_id: your-dev-gcp-project-id
schema:
  fields:
  - description: Unique identifier
    mode: REQUIRED
    name: id
    type: STRING
  - description: Record creation timestamp
    mode: NULLABLE
    name: created_at
    type: TIMESTAMP
table_id: your-dev-table-id
